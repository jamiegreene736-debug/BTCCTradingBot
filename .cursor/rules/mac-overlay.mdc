---
description: Keep Jamie's Mac unpacked overlay folder current
alwaysApply: true
---

The live unpacked Chrome overlay is:

`/Users/jamiegreene/BTCCTradingBot/chrome-extensions/bitunix-momentum-overlay`

Never use `/path/to/BTCCTradingBot`. Never run overlay git commands in
`Rental-Community-Tracker` or `Rental Comunity Tool Cursor/repo`.

Railway and cloud agents cannot write that Mac folder. A LaunchAgent at
`~/Library/LaunchAgents/com.bitunix.intraday-overlay-sync.plist` pulls
`/Users/jamiegreene/BTCCTradingBot` every two minutes. The overlay reloads
itself when `extension_version` changes. After overlay or signal-UI changes,
bump `chrome-extensions/bitunix-momentum-overlay/manifest.json` and push.
Do not ask the user to run update scripts or click Reload.
